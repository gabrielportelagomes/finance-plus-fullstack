import { ThreeDots } from 'react-loader-spinner';

export const loadingButton = (
  <ThreeDots
    height="50"
    width="50"
    radius="4"
    color="white"
    ariaLabel="three-dots-loading"
    wrapperStyle={{}}
    wrapperClassName=""
    visible={true}
  />
);
